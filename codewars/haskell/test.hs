#!/usr/bin/evn stack

printStringNTimes :: Int -> String
printStringNTimes n =
    let heu = "hue"
    do a <- "hue"
        hue = hue ++ "hue"

main = do
    a <- getLine
    let n = (read a :: Int)

    print(printStringNTimes n)
